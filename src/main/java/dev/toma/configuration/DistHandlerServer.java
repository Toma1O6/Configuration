package dev.toma.configuration;

import dev.toma.configuration.api.ModConfig;

public class DistHandlerServer implements IDistHandler {

    @Override
    public void runConfigSetup(ModConfig config) {

    }
}
